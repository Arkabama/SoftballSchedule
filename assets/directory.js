const search=document.querySelector('#team-search');const items=[...document.querySelectorAll('#team-list li')];const empty=document.querySelector('#no-teams');search?.addEventListener('input',()=>{const term=search.value.trim().toLowerCase();let visible=0;for(const item of items){item.hidden=!item.textContent.toLowerCase().includes(term);if(!item.hidden)visible++}empty.hidden=visible!==0});